apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gitea-web
  namespace: gitea
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-cloudflare"
spec:
  rules:
  - host: gitea.ugard.win
    http:
      paths:
        - path: "/"
          pathType: Prefix
          backend:
            service:
              name: gitea-web
              port:
                number: 80
  tls:
  - hosts:
    - gitea.ugard.win
    secretName: tls-gitea-ingress-http