apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - pvc.yaml
  - certificate.yaml
  - dovecot-configmap.yaml
  - dovecot.yaml
  - mbsync-configmap.yaml
  - mbsync-cronjob.yaml
  # Generated by scripts/create-mailarchive-sealed-secret.sh. Holds the two
  # upstream IMAP app passwords and the two local Dovecot login passwords.
  - sealed-secret.yaml
