apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - pvc.yaml
  - certificate.yaml
  - dovecot-configmap.yaml
  - dovecot.yaml
  - mbsync-configmap.yaml
  - mbsync-cronjob.yaml
  # sealed-secret.yaml is intentionally absent until
  # scripts/create-mailarchive-sealed-secret.sh has been run; that script
  # appends the entry here itself.
