apiVersion: v1
kind: Service
metadata:
  name: ftp
spec:
  type: ClusterIP
  ports:
    - name: service
      port: 2121
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/name: jellyfin
    app.kubernetes.io/instance: kustomize-jellyfin