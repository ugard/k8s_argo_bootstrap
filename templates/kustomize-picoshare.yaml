apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kustomize-picoshare
  namespace: argocd
spec:
  destination:
    namespace: picoshare
    server: {{ .Values.spec.destination.server }}
  project: default
  source:
    path: apps/picoshare
    repoURL: {{ .Values.spec.source.repoURL }}
    targetRevision: {{ .Values.spec.source.targetRevision }}
