apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kustomize-gotify
  namespace: argocd
spec:
  destination:
    namespace: gotify
    server: {{ .Values.spec.destination.server }}
  project: default
  source:
    path: apps/gotify
    repoURL: {{ .Values.spec.source.repoURL }}
    targetRevision: {{ .Values.spec.source.targetRevision }}